package main

import "fmt"

func main() {
    // We want to maximize the integer multiplier, so n should be minimized
    // As n > 1, the minimum is n = 2
    // As the number of digits equals 9, the most the integer multipler
    // can have is 4 digits
    for i := 9999; i > 0; i-- {
        
    }

    fmt.Println("TODO")
}
